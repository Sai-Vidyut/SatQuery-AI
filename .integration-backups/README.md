# Integration backups

Point-in-time snapshots from branch integration work.

**Committed (safe):** tracked-code `.patch` files only (no secrets).

**Ignored:** any `.env` copy, `backend.env*`, or `*dot-env*` patch under this tree — keep those local only.
